<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/edit_text_background" /> <!-- Light blue background -->
    <stroke
        android:width="1dp"
        android:color="@color/wellness_high" /> <!-- Slightly darker border -->
    <corners android:radius="10dp" /> <!-- Corner radius of 16dp -->
</shape>