<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
        <solid android:color="@color/primary_background_color" /> <!-- Light blue background -->
    <stroke
        android:width="1dp"
        android:color="@color/secondary_front_color" /> <!-- Slightly darker border -->
    <corners android:radius="12dp" /> <!-- Corner radius of 16dp -->
</shape>